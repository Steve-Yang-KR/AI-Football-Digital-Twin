document.addEventListener('DOMContentLoaded',()=>{const form=document.getElementById('simForm');const out=document.getElementById('simResult');if(!form||!out)return;form.addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(form);const payload={formation:String(fd.get('formation')||'4-3-3'),press_height:Number(fd.get('press_height')),tempo:Number(fd.get('tempo')),width:Number(fd.get('width')),fatigue:Number(fd.get('fatigue'))};out.textContent='Running scenario…';try{const r=await fetch('/api/simulate',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});if(!r.ok)throw new Error('Simulation failed');const x=await r.json();out.textContent=`Win probability: ${x.win_probability}%\nExpected goals: ${x.expected_goals}\nTransition risk: ${x.transition_risk}%\nConfidence: ${Math.round(x.confidence*100)}%\n\nRecommendation: ${x.recommendation}`;}catch(err){out.textContent=err.message;}});});