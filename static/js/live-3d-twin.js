const clamp=(v,min,max)=>Math.max(min,Math.min(max,v));

function markup(){return `<section class="live3d-section" aria-label="Live 3D Digital Twin"><div class="live3d-head"><div><small>LIVE 3D RECONSTRUCTION</small><h3>Interactive Match Hologram</h3><p>The panorama-derived Twin state is rendered as a perspective stadium with holographic players, trails and tactical overlays.</p></div><div class="live3d-actions"><button class="secondary" id="live3dReset">Reset view</button><button class="primary" id="live3dRotate">Pause rotation</button></div></div><div class="live3d-layout"><div class="live3d-stage"><canvas id="live3dCanvas" width="1280" height="720"></canvas><div class="live3d-badge" id="live3dState">WAITING FOR TWIN</div><div class="live3d-legend"><span><i class="team-a"></i>Team A</span><span><i class="team-b"></i>Team B</span><span><i class="ball"></i>Ball</span></div></div><aside class="live3d-panel"><label>3D angle <output id="live3dAngleOut">-15°</output><input id="live3dAngle" type="range" min="-30" max="5" value="-15"></label><label>Height <output id="live3dHeightOut">1.8×</output><input id="live3dHeight" type="range" min="10" max="28" value="18"></label><label>Depth <output id="live3dDepthOut">1.0×</output><input id="live3dDepth" type="range" min="70" max="150" value="100"></label><div class="live3d-metrics"><div><small>Entities</small><b id="live3dEntities">0</b></div><div><small>Confidence</small><b id="live3dConfidence">0%</b></div><div><small>Update rate</small><b id="live3dRate">0 Hz</b></div><div><small>Mode</small><b>Live Twin</b></div></div><p>This is a 2.5D reconstruction: observed field coordinates are projected into a controllable 3D holographic scene. A trained multi-view model is required for true body-pose depth.</p></aside></div></section>`}

export function initLive3DTwin(){
 const anchor=document.querySelector('.p2t-section')||document.querySelector('.holo-section');if(!anchor)return;
 anchor.insertAdjacentHTML('afterend',markup());
 const canvas=document.getElementById('live3dCanvas'),ctx=canvas.getContext('2d');
 let tracks=[],confidence=0,rate=0,angle=-15,height=1.8,depth=1,autoRotate=true,phase=0;
 const angleInput=document.getElementById('live3dAngle'),heightInput=document.getElementById('live3dHeight'),depthInput=document.getElementById('live3dDepth');
 function sync(){angle=+angleInput.value;height=+heightInput.value/10;depth=+depthInput.value/100;document.getElementById('live3dAngleOut').textContent=`${angle}°`;document.getElementById('live3dHeightOut').textContent=`${height.toFixed(1)}×`;document.getElementById('live3dDepthOut').textContent=`${depth.toFixed(1)}×`}
 [angleInput,heightInput,depthInput].forEach(i=>i.addEventListener('input',sync));sync();
 document.getElementById('live3dReset').onclick=()=>{angleInput.value=-15;heightInput.value=18;depthInput.value=100;phase=0;sync()};
 document.getElementById('live3dRotate').onclick=e=>{autoRotate=!autoRotate;e.currentTarget.textContent=autoRotate?'Pause rotation':'Resume rotation'};
 window.addEventListener('panorama-twin-state',e=>{const d=e.detail||{};tracks=d.tracks||[];confidence=d.confidence||0;rate=d.rate||0;document.getElementById('live3dEntities').textContent=tracks.length;document.getElementById('live3dConfidence').textContent=`${confidence}%`;document.getElementById('live3dRate').textContent=`${rate} Hz`;document.getElementById('live3dState').textContent=tracks.length?'LIVE RECONSTRUCTION':'WAITING FOR TWIN'});
 const project=(x,y,z=0)=>{const yaw=(angle+(autoRotate?Math.sin(phase)*5:0))*Math.PI/180;const nx=(x-.5)*2,ny=(y-.5)*2;const rx=nx*Math.cos(yaw)-ny*Math.sin(yaw),ry=nx*Math.sin(yaw)+ny*Math.cos(yaw);const perspective=1/(1+ry*.28*depth);return{x:canvas.width/2+rx*480*perspective,y:510+ry*210*perspective-z*height*42*perspective,s:perspective}};
 function line(a,b,alpha=.7,w=2){ctx.strokeStyle=`rgba(52,220,255,${alpha})`;ctx.lineWidth=w;ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.stroke()}
 function drawPitch(){const c=[project(0,0),project(1,0),project(1,1),project(0,1)];ctx.fillStyle='rgba(0,55,85,.55)';ctx.beginPath();ctx.moveTo(c[0].x,c[0].y);c.slice(1).forEach(p=>ctx.lineTo(p.x,p.y));ctx.closePath();ctx.fill();c.forEach((p,i)=>line(p,c[(i+1)%4],.95,3));for(let i=1;i<10;i++){line(project(i/10,0),project(i/10,1),.22,1)}for(let i=1;i<7;i++){line(project(0,i/7),project(1,i/7),.22,1)}line(project(.5,0),project(.5,1),.85,2);const center=project(.5,.5),r=58*center.s;ctx.strokeStyle='rgba(80,230,255,.8)';ctx.lineWidth=2;ctx.beginPath();ctx.ellipse(center.x,center.y,r,r*.45,0,0,Math.PI*2);ctx.stroke();}
 function player(t,now){const x=clamp((t.x-24)/(1050-48),0,1),y=clamp((t.y-24)/(680-48),0,1),p=project(x,y,0),head=project(x,y,.72),color=t.id%2?'70,200,255':'255,80,190';ctx.shadowColor=`rgb(${color})`;ctx.shadowBlur=18;ctx.strokeStyle=`rgba(${color},.95)`;ctx.lineWidth=4*p.s;ctx.beginPath();ctx.moveTo(p.x,p.y);ctx.lineTo(head.x,head.y);ctx.stroke();ctx.beginPath();ctx.arc(head.x,head.y-7*p.s,7*p.s,0,Math.PI*2);ctx.fillStyle=`rgba(${color},.8)`;ctx.fill();ctx.shadowBlur=0;ctx.fillStyle='#dffcff';ctx.font=`700 ${Math.max(9,12*p.s)}px system-ui`;ctx.textAlign='center';ctx.fillText(t.id,head.x,head.y-18*p.s);const trail=project(clamp(x-Math.sin((now+t.id*90)/900)*.035,0,1),clamp(y+.04,0,1),0);line(p,trail,.25,2)}
 function draw(now){phase+=.004;ctx.clearRect(0,0,canvas.width,canvas.height);const bg=ctx.createRadialGradient(640,460,30,640,430,700);bg.addColorStop(0,'rgba(0,100,140,.22)');bg.addColorStop(1,'rgba(0,5,18,1)');ctx.fillStyle=bg;ctx.fillRect(0,0,canvas.width,canvas.height);drawPitch();tracks.forEach(t=>player(t,now));for(let y=40;y<680;y+=16){ctx.strokeStyle='rgba(40,210,255,.045)';ctx.beginPath();ctx.moveTo(20,y);ctx.lineTo(1260,y);ctx.stroke()}requestAnimationFrame(draw)}requestAnimationFrame(draw)
}
