services:
  - type: web
    name: football-digital-twin-os
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn hologram_app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/health
    autoDeploy: true
